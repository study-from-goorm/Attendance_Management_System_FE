function PageTitle({ title }) {
  return (
    <>
      <h2 className="text-primary-color font-semibold text-2xl">{title}</h2>
      {/* bread crumb 추가 예정 */}
    </>
  );
}

export default PageTitle;
